import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ['laptopserver', 'laptopserver.local', 'uncountrified-selah-semipathologically.ngrok-free.dev'],
};

export default nextConfig;
